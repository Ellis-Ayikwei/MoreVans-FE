export interface ServiceRequest {
    // ... existing fields ...
    selected_price?: number;
    staff_count?: number;
    selected_date?: string;
    request_id?: string;
    // ... existing fields ...
}
